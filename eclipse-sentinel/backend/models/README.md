# This directory is reserved for future ML model files.
# Currently, the prototype uses:
#   - OpenCV Haar cascades (bundled with opencv-python)
#   - Tesseract OCR (system dependency)
#   - DeepFace (downloads models on first use)
